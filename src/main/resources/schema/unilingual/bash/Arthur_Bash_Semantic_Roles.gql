define

##########---------- Entities ----------##########

#####----- Bash -----#####
BashSourceArtifact sub SourceArtifact;

AmpersandGreaterThanArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ANDANDArtifact sub BashSourceArtifact,
	# Semantic
	plays AND:IS_AND,
	plays BOOLEAN:IS_BOOLEAN,
	plays OPERATOR:IS_OPERATOR;

ANDArtifact sub BashSourceArtifact,
	# Semantic
	plays AND:IS_AND,
	plays BOOLEAN:IS_BOOLEAN,
	plays OPERATOR:IS_OPERATOR;

ArithCompundComparisionArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArithEQEQArtifact sub BashSourceArtifact,
	# Semantic
	plays EQUAL:IS_EQUAL,
	plays OPERATOR:IS_OPERATOR,
	plays RELATIONAL:IS_RELATIONAL;

ArithGTArtifact sub BashSourceArtifact,
	# Semantic
	plays ARITHMETIC:IS_ARITHMETIC,
	plays OPERATOR:IS_OPERATOR,
	plays RIGHT_SHIFT:IS_RIGHT_SHIFT;

ArithLTArtifact sub BashSourceArtifact,
	# Semantic
	plays ARITHMETIC:IS_ARITHMETIC,
	plays LEFT_SHIFT:IS_LEFT_SHIFT,
	plays OPERATOR:IS_OPERATOR;

ArithmeticBaseCharLeftParenthesesHashRightParenthesesArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArithmeticBitwiseAndArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArithmeticBitwiseOrArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArithmeticBitwiseXorArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArithmeticCommandArtifact sub BashSourceArtifact,
	# Semantic
	plays ARITHMETIC:IS_ARITHMETIC,
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY,
	plays CONDITION:IS_CONDITION,
	plays ELSE:IS_ELSE,
	plays EXPRESSION:IS_EXPRESSION,
	plays IF:IS_IF,
	plays INCOMPLETE:IS_INCOMPLETE,
	plays RIGHT:IS_RIGHT,
	plays WHILE:IS_WHILE;

ArithmeticEqualityArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArithmeticLogicAndArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArithmeticMultiplicationArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArithmeticParenthesisExprArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArithmeticPostIncrArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArithmeticPreIncrArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArithmeticShiftArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArithmeticSimpleArtifact sub BashSourceArtifact,
	# Semantic
	plays ARITHMETIC:IS_ARITHMETIC,
	plays EXPRESSION:IS_EXPRESSION,
	plays INCOMPLETE:IS_INCOMPLETE;

ArithmeticSumArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArithmeticTernaryOperatorArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ArrayAssignmentListArtifact sub BashSourceArtifact,
	# Semantic
	plays RIGHT:IS_RIGHT;

AssignmentWordArtifact sub BashSourceArtifact,
	# Semantic
	plays ASSIGNMENT:IS_ASSIGNMENT,
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY,
	plays LEFT:IS_LEFT,
	plays OPERATOR:IS_OPERATOR,
	plays THEN:IS_THEN,
	plays UNANNOTATED:IS_UNANNOTATED;

AsteriskArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

BackquoteArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE;

BackquoteShellcommandArtifact sub BashSourceArtifact,
	# Semantic
	plays CALL:IS_CALL,
	plays EXPRESSION:IS_EXPRESSION,
	plays INCOMPLETE:IS_INCOMPLETE,
	plays LITERAL:IS_LITERAL,
	plays STRING:IS_STRING;

BADCHARACTERArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

CaretArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

CasePatternArtifact sub BashSourceArtifact,
	# Semantic
	plays CASE:IS_CASE,
	plays CONDITION:IS_CONDITION,
	plays EXPRESSION:IS_EXPRESSION,
	plays STATEMENT:IS_STATEMENT,
	plays SWITCH:IS_SWITCH;

CasePatternListArtifact sub BashSourceArtifact,
	# Semantic
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY,
	plays CASE:IS_CASE;

COLONArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE;

CombinedWordArtifact sub BashSourceArtifact,
	# Semantic
	plays ASSIGNMENT:IS_ASSIGNMENT,
	plays EXPRESSION:IS_EXPRESSION,
	plays FOR:IS_FOR,
	plays IDENTIFIER:IS_IDENTIFIER,
	plays INCOMPLETE:IS_INCOMPLETE,
	plays LEFT:IS_LEFT,
	plays RIGHT:IS_RIGHT,
	plays UPDATE:IS_UPDATE;

CommaArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

CommentArtifact sub BashSourceArtifact,
	# Semantic
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY,
	plays COMMENT:IS_COMMENT,
	plays ELSE:IS_ELSE,
	plays NOOP:IS_NOOP,
	plays THEN:IS_THEN;

ComposedCommandArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ComposedVariableArtifact sub BashSourceArtifact,
	# Semantic
	plays EXPRESSION:IS_EXPRESSION,
	plays IDENTIFIER:IS_IDENTIFIER,
	plays INCOMPLETE:IS_INCOMPLETE,
	plays VARIABLE:IS_VARIABLE;

ConditionalShellcommandArtifact sub BashSourceArtifact,
	# Semantic
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY,
	plays CONDITION:IS_CONDITION,
	plays ELSE:IS_ELSE,
	plays EXPRESSION:IS_EXPRESSION,
	plays IF:IS_IF,
	plays THEN:IS_THEN,
	plays WHILE:IS_WHILE;

CondOpArtifact sub BashSourceArtifact,
	# Semantic
	plays EQUAL:IS_EQUAL,
	plays GREATER_THAN:IS_GREATER_THAN,
	plays GREATER_THAN_OR_EQUAL:IS_GREATER_THAN_OR_EQUAL,
	plays INCOMPLETE:IS_INCOMPLETE,
	plays LESS_THAN:IS_LESS_THAN,
	plays LESS_THAN_OR_EQUAL:IS_LESS_THAN_OR_EQUAL,
	plays NOT:IS_NOT,
	plays OPERATOR:IS_OPERATOR,
	plays RELATIONAL:IS_RELATIONAL,
	plays UNANNOTATED:IS_UNANNOTATED;

CondOpEQEQArtifact sub BashSourceArtifact,
	# Semantic
	plays EQUAL:IS_EQUAL,
	plays EXPRESSION:IS_EXPRESSION,
	plays OPERATOR:IS_OPERATOR,
	plays RELATIONAL:IS_RELATIONAL;

CondOpEqualsTildeArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

CondOpNOTArtifact sub BashSourceArtifact,
	# Semantic
	plays BOOLEAN:IS_BOOLEAN,
	plays NOT:IS_NOT,
	plays OPERATOR:IS_OPERATOR;

DOLLARArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE;

ElifArtifact sub BashSourceArtifact,
	# Semantic
	plays ELSE:IS_ELSE,
	plays IF:IS_IF,
	plays STATEMENT:IS_STATEMENT;

ElseArtifact sub BashSourceArtifact,
	# Semantic
	plays ELSE:IS_ELSE,
	plays STATEMENT:IS_STATEMENT;

EQArtifact sub BashSourceArtifact,
	# Semantic
	plays ASSIGNMENT:IS_ASSIGNMENT,
	plays OPERATOR:IS_OPERATOR;

EqualsApostropheArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ERRORELEMENTArtifact sub BashSourceArtifact,
	# Semantic
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY,
	plays INCOMPLETE:IS_INCOMPLETE,
	plays THEN:IS_THEN;

ExclamationArtifact sub BashSourceArtifact,
	# Semantic
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY,
	plays CONDITION:IS_CONDITION,
	plays ELSE:IS_ELSE,
	plays IF:IS_IF,
	plays THEN:IS_THEN,
	plays UNANNOTATED:IS_UNANNOTATED;

FILEArtifact sub BashSourceArtifact,
	# Semantic
	plays FILE:IS_FILE;

FileRefArtifact sub BashSourceArtifact,
	# Semantic
	plays IDENTIFIER:IS_IDENTIFIER,
	plays IMPORT:IS_IMPORT,
	plays PATHNAME:IS_PATHNAME;

ForShellcommandArtifact sub BashSourceArtifact,
	# Semantic
	plays FOR:IS_FOR,
	plays STATEMENT:IS_STATEMENT;

ForwardSlashArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

FunctionDefElementArtifact sub BashSourceArtifact,
	# Semantic
	plays DECLARATION:IS_DECLARATION,
	plays FUNCTION:IS_FUNCTION;

GenericBashCommandArtifact sub BashSourceArtifact,
	# Semantic
	plays EXPRESSION:IS_EXPRESSION,
	plays INCOMPLETE:IS_INCOMPLETE;

GreaterThanAmpersandArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

GreaterThanPipeArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

GroupElementArtifact sub BashSourceArtifact,
	# Semantic
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY;

GTArtifact sub BashSourceArtifact,
	# Semantic
	plays GREATER_THAN:IS_GREATER_THAN,
	plays OPERATOR:IS_OPERATOR,
	plays RELATIONAL:IS_RELATIONAL;

GTEQArtifact sub BashSourceArtifact,
	# Semantic
	plays GREATER_THAN_OR_EQUAL:IS_GREATER_THAN_OR_EQUAL,
	plays OPERATOR:IS_OPERATOR,
	plays RELATIONAL:IS_RELATIONAL;

GTGTArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE,
	plays OPERATOR:IS_OPERATOR;

HereDocContentArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

HeredocContentElementArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

HeredocEndElementArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

HeredocEndElementIgnoringTabsArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

HeredocEndMarkerArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

HeredocEndMarkerIgnoringTabsArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

HeredocMarkerTagArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

HeredocStartElementArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

HeredocStartMarkerArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

HyphenApostropheArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

HyphenArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

HyphenEqualsArithmeticArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

HyphenHyphenArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

IfShellcommandArtifact sub BashSourceArtifact,
	# Semantic
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY,
	plays ELSE:IS_ELSE,
	plays IF:IS_IF,
	plays STATEMENT:IS_STATEMENT,
	plays THEN:IS_THEN;

IncludeCommandArtifact sub BashSourceArtifact,
	# Semantic
	plays IMPORT:IS_IMPORT,
	plays STATEMENT:IS_STATEMENT;

IntLiteralArtifact sub BashSourceArtifact,
	# Semantic
	plays LITERAL:IS_LITERAL,
	plays NUMBER:IS_NUMBER,
	plays PRIMITIVE:IS_PRIMITIVE;

LazyLetExprArtifact sub BashSourceArtifact,
	# Semantic
	plays ASSIGNMENT:IS_ASSIGNMENT,
	plays EXPRESSION:IS_EXPRESSION,
	plays INCOMPLETE:IS_INCOMPLETE;

LBForArithmeticArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE;

LBLBLeftBracketArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE;

LBLeftSquareArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE;

LessThanGreaterThanArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

LessThanLessThanLessThanArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

LetArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE,
	plays STATEMENT:IS_STATEMENT;

LetCommandArtifact sub BashSourceArtifact,
	# Semantic
	plays ASSIGNMENT:IS_ASSIGNMENT,
	plays EXPRESSION:IS_EXPRESSION,
	plays INCOMPLETE:IS_INCOMPLETE;

LogicalBlockArtifact sub BashSourceArtifact,
	# Semantic
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY,
	plays ELSE:IS_ELSE,
	plays FOR:IS_FOR,
	plays THEN:IS_THEN,
	plays WHILE:IS_WHILE;

LPLPArtifact sub BashSourceArtifact,
	# Semantic
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY,
	plays ELSE:IS_ELSE,
	plays EXPRESSION:IS_EXPRESSION,
	plays FOR:IS_FOR,
	plays INCOMPLETE:IS_INCOMPLETE,
	plays ITERATOR:IS_ITERATOR,
	plays THEN:IS_THEN;

LTArtifact sub BashSourceArtifact,
	# Semantic
	plays LESS_THAN:IS_LESS_THAN,
	plays OPERATOR:IS_OPERATOR,
	plays RELATIONAL:IS_RELATIONAL;

LTEQArtifact sub BashSourceArtifact,
	# Semantic
	plays LESS_THAN_OR_EQUAL:IS_LESS_THAN_OR_EQUAL,
	plays OPERATOR:IS_OPERATOR,
	plays RELATIONAL:IS_RELATIONAL;

NamedSymbolArtifact sub BashSourceArtifact,
	# Semantic
	plays IDENTIFIER:IS_IDENTIFIER,
	plays NAME:IS_NAME;

NOTEQArtifact sub BashSourceArtifact,
	# Semantic
	plays EQUAL:IS_EQUAL,
	plays NOT:IS_NOT,
	plays OPERATOR:IS_OPERATOR,
	plays RELATIONAL:IS_RELATIONAL;

NumberArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

NumrangeFiledescriptorArtifact sub BashSourceArtifact,
	# Semantic
	plays IDENTIFIER:IS_IDENTIFIER,
	plays INCOMPLETE:IS_INCOMPLETE,
	plays RECEIVER:IS_RECEIVER;

OctalLiteralArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ORArtifact sub BashSourceArtifact,
	# Semantic
	plays BOOLEAN:IS_BOOLEAN,
	plays OPERATOR:IS_OPERATOR,
	plays OR:IS_OR;

ORORArtifact sub BashSourceArtifact,
	# Semantic
	plays BOOLEAN:IS_BOOLEAN,
	plays OPERATOR:IS_OPERATOR,
	plays OR:IS_OR;

ParameterExpansionOperatorApostropheAsteriskApostropheArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ParameterExpansionOperatorApostropheEqualsApostropheArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ParameterExpansionOperatorApostropheExclamationApostropheArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ParameterExpansionOperatorApostropheHyphenApostropheArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ParameterExpansionOperatorApostrophePlusApostropheArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ParameterExpansionOperatorApostropheQuestionApostropheArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ParameterExpansionOperatorUnknownArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ParamExpATArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE,
	plays OPERATOR:IS_OPERATOR;

ParamExpCOLONArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE,
	plays OPERATOR:IS_OPERATOR;

ParamExpNUMArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE,
	plays OPERATOR:IS_OPERATOR;

ParamExpNUMNUMArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE,
	plays OPERATOR:IS_OPERATOR;

ParamExpPERCArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE,
	plays OPERATOR:IS_OPERATOR;

ParamExpSLASHArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE,
	plays OPERATOR:IS_OPERATOR;

PercentArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

PipeAmpersandArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

PipelineCommandArtifact sub BashSourceArtifact,
	# Semantic
	plays CALL:IS_CALL,
	plays CONDITION:IS_CONDITION,
	plays ELSE:IS_ELSE,
	plays EXPRESSION:IS_EXPRESSION,
	plays IF:IS_IF,
	plays INCOMPLETE:IS_INCOMPLETE,
	plays WHILE:IS_WHILE;

PlusApostropheArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

PlusArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

PlusEqualsArithmeticArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

PlusEqualsArtifact sub BashSourceArtifact,
	# Semantic
	plays ASSIGNMENT:IS_ASSIGNMENT,
	plays OPERATOR:IS_OPERATOR,
	plays UNANNOTATED:IS_UNANNOTATED;

PlusPlusArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

ProcessSubstitutionElementArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

QuestionApostropheArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

QuestionArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

RBForArithmeticArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE;

RBRBRightBracketArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE;

RBRightSquareArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE;

RedirectElementArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE;

RedirectListArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE;

RPRPArtifact sub BashSourceArtifact,
	# Semantic
	plays EXPRESSION:IS_EXPRESSION,
	plays FOR:IS_FOR,
	plays INCOMPLETE:IS_INCOMPLETE,
	plays UPDATE:IS_UPDATE;

SelectArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

SelectCommandArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

SEMICOLONSEMICOLONArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE;

SimpleCommandArtifact sub BashSourceArtifact,
	# Semantic
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY,
	plays CONDITION:IS_CONDITION,
	plays ELSE:IS_ELSE,
	plays EXPRESSION:IS_EXPRESSION,
	plays IF:IS_IF,
	plays THEN:IS_THEN,
	plays WHILE:IS_WHILE;

SingleBashExpansionArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

StringArtifact sub BashSourceArtifact,
	# Semantic
	plays ASSIGNMENT:IS_ASSIGNMENT,
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY,
	plays EXPRESSION:IS_EXPRESSION,
	plays FOR:IS_FOR,
	plays LEFT:IS_LEFT,
	plays LITERAL:IS_LITERAL,
	plays PRIMITIVE:IS_PRIMITIVE,
	plays RIGHT:IS_RIGHT,
	plays STRING:IS_STRING,
	plays THEN:IS_THEN,
	plays UPDATE:IS_UPDATE;

StringContentArtifact sub BashSourceArtifact,
	# Semantic
	plays EXPRESSION:IS_EXPRESSION,
	plays LITERAL:IS_LITERAL,
	plays STRING:IS_STRING;

SubshellShellcommandArtifact sub BashSourceArtifact,
	# Semantic
	plays CALL:IS_CALL,
	plays CONDITION:IS_CONDITION,
	plays EXPRESSION:IS_EXPRESSION,
	plays INCOMPLETE:IS_INCOMPLETE,
	plays WHILE:IS_WHILE;

ThenArtifact sub BashSourceArtifact,
	# Semantic
	plays IF:IS_IF,
	plays STATEMENT:IS_STATEMENT,
	plays THEN:IS_THEN;

TimeArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

TimeWithOptionalPArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

TrapArtifact sub BashSourceArtifact,
	# Semantic
	plays UNANNOTATED:IS_UNANNOTATED;

UnevaluatedString2Artifact sub BashSourceArtifact,
	# Semantic
	plays EXPRESSION:IS_EXPRESSION,
	plays LITERAL:IS_LITERAL,
	plays STRING:IS_STRING;

UntilLoopArtifact sub BashSourceArtifact,
	# Semantic
	plays INCOMPLETE:IS_INCOMPLETE,
	plays STATEMENT:IS_STATEMENT,
	plays WHILE:IS_WHILE;

VarDefElementArtifact sub BashSourceArtifact,
	# Semantic
	plays ASSIGNMENT:IS_ASSIGNMENT,
	plays BLOCK:IS_BLOCK,
	plays BODY:IS_BODY,
	plays DECLARATION:IS_DECLARATION,
	plays ELSE:IS_ELSE,
	plays EXPRESSION:IS_EXPRESSION,
	plays FOR:IS_FOR,
	plays ITERATOR:IS_ITERATOR,
	plays THEN:IS_THEN,
	plays VARIABLE:IS_VARIABLE;

VariableArtifact sub BashSourceArtifact,
	# Semantic
	plays EXPRESSION:IS_EXPRESSION,
	plays IDENTIFIER:IS_IDENTIFIER,
	plays VARIABLE:IS_VARIABLE;

VarSubstitutionArtifact sub BashSourceArtifact,
	# Semantic
	plays EXPRESSION:IS_EXPRESSION,
	plays IDENTIFIER:IS_IDENTIFIER,
	plays INCOMPLETE:IS_INCOMPLETE,
	plays VARIABLE:IS_VARIABLE;

VarUseElementArtifact sub BashSourceArtifact,
	# Semantic
	plays ASSIGNMENT:IS_ASSIGNMENT,
	plays EXPRESSION:IS_EXPRESSION,
	plays IDENTIFIER:IS_IDENTIFIER,
	plays LEFT:IS_LEFT,
	plays VARIABLE:IS_VARIABLE;

WhileLoopArtifact sub BashSourceArtifact,
	# Semantic
	plays STATEMENT:IS_STATEMENT,
	plays WHILE:IS_WHILE;

WordArtifact sub BashSourceArtifact,
	# Semantic
	plays EXPRESSION:IS_EXPRESSION,
	plays IDENTIFIER:IS_IDENTIFIER;

##########---------- Semantic Roles ----------##########
AND sub relation,
	relates IS_AND;

ARITHMETIC sub relation,
	relates IS_ARITHMETIC;

ASSIGNMENT sub relation,
	relates IS_ASSIGNMENT;

BLOCK sub relation,
	relates IS_BLOCK;

BODY sub relation,
	relates IS_BODY;

BOOLEAN sub relation,
	relates IS_BOOLEAN;

CALL sub relation,
	relates IS_CALL;

CASE sub relation,
	relates IS_CASE;

COMMENT sub relation,
	relates IS_COMMENT;

CONDITION sub relation,
	relates IS_CONDITION;

DECLARATION sub relation,
	relates IS_DECLARATION;

ELSE sub relation,
	relates IS_ELSE;

EQUAL sub relation,
	relates IS_EQUAL;

EXPRESSION sub relation,
	relates IS_EXPRESSION;

FILE sub relation,
	relates IS_FILE;

FOR sub relation,
	relates IS_FOR;

FUNCTION sub relation,
	relates IS_FUNCTION;

GREATER_THAN sub relation,
	relates IS_GREATER_THAN;

GREATER_THAN_OR_EQUAL sub relation,
	relates IS_GREATER_THAN_OR_EQUAL;

IDENTIFIER sub relation,
	relates IS_IDENTIFIER;

IF sub relation,
	relates IS_IF;

IMPORT sub relation,
	relates IS_IMPORT;

INCOMPLETE sub relation,
	relates IS_INCOMPLETE;

ITERATOR sub relation,
	relates IS_ITERATOR;

LEFT sub relation,
	relates IS_LEFT;

LEFT_SHIFT sub relation,
	relates IS_LEFT_SHIFT;

LESS_THAN sub relation,
	relates IS_LESS_THAN;

LESS_THAN_OR_EQUAL sub relation,
	relates IS_LESS_THAN_OR_EQUAL;

LITERAL sub relation,
	relates IS_LITERAL;

NAME sub relation,
	relates IS_NAME;

NOOP sub relation,
	relates IS_NOOP;

NOT sub relation,
	relates IS_NOT;

NUMBER sub relation,
	relates IS_NUMBER;

OPERATOR sub relation,
	relates IS_OPERATOR;

OR sub relation,
	relates IS_OR;

PATHNAME sub relation,
	relates IS_PATHNAME;

PRIMITIVE sub relation,
	relates IS_PRIMITIVE;

RECEIVER sub relation,
	relates IS_RECEIVER;

RELATIONAL sub relation,
	relates IS_RELATIONAL;

RIGHT sub relation,
	relates IS_RIGHT;

RIGHT_SHIFT sub relation,
	relates IS_RIGHT_SHIFT;

STATEMENT sub relation,
	relates IS_STATEMENT;

STRING sub relation,
	relates IS_STRING;

SWITCH sub relation,
	relates IS_SWITCH;

THEN sub relation,
	relates IS_THEN;

UNANNOTATED sub relation,
	relates IS_UNANNOTATED;

UPDATE sub relation,
	relates IS_UPDATE;

VARIABLE sub relation,
	relates IS_VARIABLE;

WHILE sub relation,
	relates IS_WHILE;
